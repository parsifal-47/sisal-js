function foo(N:integer; T:integer  returns integer)
   for initial
      i := 0;
      s := 0;
      t := T
   repeat
      s := old s + old i;
      i := old i + t
   while ( s <= N )
      returns value of s
   end for
end function
