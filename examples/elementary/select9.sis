function foo(a:integer; b:integer  returns integer)
   if ( (a > b) & (a ~= 0) ) then
      a - b
   elseif ( ((b > a) & (b ~= 0)) | ((a = b) & (a = 0)) ) then
      b - a
   else
      0
   end if
end function
